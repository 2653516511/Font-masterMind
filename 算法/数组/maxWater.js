const { reject } = require("../../../try/sourceUnder");

/**
 * 接雨水
 * @param {array} arr 
 */
const maxWater = (arr) => {
    // 边界条件
    if(arr.length <= 2) {
        return 0
    }

    // 主逻辑
    

    // 返回值 number
}
const arr = [0,1,0,2,1,0,1,3,2,1,2,1]
console.log(maxWater(arr));
